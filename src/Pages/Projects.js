// Projects.js
import React from 'react';
import ProjectList from '../Components/ProjectList';

const Projects = () => {
  return <ProjectList />;
};

export default Projects; // `export default` kullanıyoruz
